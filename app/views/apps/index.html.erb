<h1>Apps#index</h1>
<p>Find me in app/views/apps/index.html.erb</p>

<div class="row">
	<div class="col-md-4">
		<table>
		<% @rss_free_kr.entries.each do |entry| %>
		  <tr>
		    <td><a href="<%= entry.link %>"><%= entry.title.force_encoding("UTF-8") %></a></td>
		    <td></td>
		  </tr>
		<% end %>
		</table>
	</div>

	<div class="col-md-4">
		<table>
		<% @rss_free_jp.entries.each do |entry| %>
		  <tr>
		    <td><a href="<%= entry.link %>"><%= entry.title.force_encoding("UTF-8") %></a></td>
		    <td></td>
		  </tr>
		<% end %>
		</table>
	</div>

	<div class="col-md-4">
		<table>
		<% @rss_free_us.entries.each do |entry| %>
		  <tr>
		    <td><a href="<%= entry.link %>"><%= entry.title.force_encoding("UTF-8") %></a></td>
		    <td></td>
		  </tr>
		<% end %>
		</table>
	</div>
</div>


